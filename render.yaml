# Render Blueprint - E-Commerce Microservices
# Single Web Service con Docker

services:
  - type: web
    name: ecommerce-microservices
    runtime: docker
    dockerfilePath: ./Dockerfile.all-in-one
    region: oregon
    plan: free
    healthCheckPath: /
    
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: JWT_KEY
        generateValue: true
      - key: DATABASE_URL
        value: "postgresql://neondb_owner:npg_U3ToW0RsVOSc@ep-falling-art-a8l470fw-pooler.eastus2.azure.neon.tech/neondb?sslmode=require&channel_binding=require"
